// resources/ts/app.ts
//
// Entry point — compiled by Bun into public/build/js/app.js.
// Placeholder: real content depth (bare vs. a real starting point) is
// still an open decision. This just proves the pipeline works end-to-end.

console.log("vwork app.ts loaded");
